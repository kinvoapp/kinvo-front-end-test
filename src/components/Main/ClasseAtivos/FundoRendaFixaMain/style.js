import styled from 'styled-components'

export const RenderDashboard = styled.main`
  
  grid-area: main;
  padding: 20px 15px;
  background-color: var(--bg-main);  
  `

export const Title = styled.h1`

  color: var(--text-color-component-main);
`

export const ContainerValuesDisplay = styled.div`

  display: flex;
  justify-content: space-between;
  align-items: center;
  margin: 10px 0;
`