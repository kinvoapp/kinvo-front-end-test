import { HEADER_HEIGHT } from '@components/kinvoHeader/KinvoHeader.styles';
import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  flex: 1;
  margin-top: ${HEADER_HEIGHT}px;
`;
